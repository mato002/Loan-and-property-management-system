<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allows http:// LAN / emulator URLs when CAPACITOR_ALLOW_CLEARTEXT=true (dev only). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
